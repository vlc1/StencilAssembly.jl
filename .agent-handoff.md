# Agent Handoff

## Completed Work

Two refactor passes are sitting in the working tree (uncommitted):

1. **1-D range-based refactor.** Switched row/col from
   `CartesianRuns.CartesianRunIndices{1}` to `NTuple{1, AbstractUnitRange{Int}}`.
   Dropped `CartesianRuns` from `[deps]`. Rewrote tests, reference oracle,
   and docs accordingly. Holed-mask support is intentionally gone — row/col
   are rectangular by design.
2. **Dispatch / kernel-restructure pass on `src/stencil.jl`.** Pinned
   `D = 1` and `N = 1` in the 1-D `assemble` / `update!` signatures (pure
   dispatch, no runtime guard). Added `D ≤ N` invariant at the inner
   constructor. Added `build(st, row, col)` one-shot convenience. Dropped
   the `::Type{SparseMatrixCSC{T,Int}}` trailing arg (YAGNI). Deleted the
   `_kbounds` helper. Restructured `_pattern!` and `_fill!` as `K` outer
   loops with the counting-sort-CSC mutate-restore-`colptr` pattern —
   allocation-free; no per-cell branching.

Test suite: `julia --project=. -e 'using Pkg; Pkg.test()'` — 31 assertions
across 8 testsets, all green.

## Key Decisions

- **Row/col representation**: `NTuple{N, AbstractUnitRange{Int}}` on a shared
  integer mesh. Rectangular sub-blocks; unequal lengths and shifted ranges
  supported. Arbitrary boolean masks are out of scope.
- **Coefficient indexing**: column-anchored, `coefs[k][c]` at the column mesh
  position. Caller's responsibility for axes covering `col` (wrap with
  `OffsetArray` if needed).
- **Strict-descending offsets** is the CSC-sortedness invariant; enforced at
  the `LinearStencil` inner constructor via `issorted(offsets; lt = >=)`.
- **`D ≤ N`** is enforced at the inner constructor (stencil dim must fit
  within coef-array dims).
- **Kernel shape**: `K` outer loops, one per offset; counting-sort-CSC with
  `colptr` mutated in place as slot tracker, restored via shift-right at
  the end. Single-threaded; mid-fill `colptr` is briefly inconsistent.
- **API surface**: `LinearStencil`, `assemble`, `update!`, `build` (exported);
  `_pattern!`, `_fill!` internal.
- **For the upcoming N-D milestone**: tuple-length dispatch (`Base.front` /
  `last`), separate methods for `NTuple{1, …}` base and `NTuple{Nd, …}`
  recursive. No `Ref`s — accumulators returned as `Tuple`s. K-outer-loops
  shape generalises (each `k` becomes a sub-stencil with a rectangular
  c-range).
- **Symbolic/numeric split (`StencilPattern` vs `LinearStencil`)** considered
  and **deferred** to the N-D design pass.

## Resume Context

- **Branch**: `main`.
- **Uncommitted in working tree** (from the two refactor passes above):
  `AGENTS.md`, `Project.toml`, `docs/plan.md`, `README.md`,
  `src/CartesianOperators.jl`, `src/stencil.jl`, `test/oracle.jl`,
  `test/reference.jl`, `test/runtests.jl`, `test/test_stencil.jl`.
- **Untracked**: `.agent-handoff.md` (this file), `src/.stencil.jl.swp`
  (vim swap from an earlier session — safe to ignore or delete).
- **Last committed state**: `f4fb65f - New LinearStencil design - CI action
  fixed` — predates both refactors. Anything in HEAD is the pre-refactor
  shape (CartesianRuns-based, etc.).
- **Tests pass** at the working-tree state. Running `Pkg.test()` requires
  no manifest update.
- **Plan files referenced during the work** (outside the repo, in your
  user dir):
  - `~/.claude/plans/kind-weaving-treehouse.md` — the most recent plan
    (the dispatch / kernel-restructure pass). The 1-D range-based plan
    was overwritten by this one; recover from git history if needed.

## Next Action

Pick one:

1. **Commit the uncommitted work.** Per `AGENTS.md`, commits are never
   automatic. Recommended split into two commits:
   - Commit A: 1-D range-based refactor (drop `CartesianRuns`, rewrite
     kernels, tests, reference oracle, docs).
   - Commit B: dispatch / kernel-restructure pass (pure dispatch on
     `D=1`, `D ≤ N` invariant, `build`, drop `::Type` arg, K-outer-loops
     kernels, doc/test updates).

   Both refactors touch the same files, so splitting requires `git add -p`
   or staging by hunks. Alternative: one combined commit with a
   composite message.

2. **Move on to the N-D milestone** (2-D first, then 3-D). The plan in
   `docs/plan.md` sketches the recursive structure (tuple-length dispatch
   via `Base.front` / `last`; K-outer-loops generalise naturally per
   dim). Implementation order per prior agreement: 2-D D=1 → 2-D D=2 →
   3-D D=1 → 3-D D=2 → 3-D D=3, validating each via tests.

3. **Revisit the symbolic/numeric split** (`StencilPattern` + `LinearStencil`)
   — deferred to N-D consideration. Three alternatives were sketched in
   conversation: (1) two-layer split, (2) term-based `NTuple{K,StencilTerm}`,
   (3) pure symbolic with coefs at call site. Option 1 was tentatively
   recommended.

## Repository pointers (read first if resuming cold)

- `AGENTS.md` — design conventions and sticky decisions.
- `docs/plan.md` — forward-looking implementation plan with status and
  roadmap.
- `README.md` — user-facing intro.
- `src/stencil.jl` — `LinearStencil` type + kernels + public API.
- `test/test_stencil.jl` — coverage map (constructor, kernels,
  integration, dispatch, `build`).
- `test/reference.jl` — brute-force `stencil_reference` oracle.

<!-- session-handoff snapshot 2026-05-15T21:08:41Z -->
<!-- branch: main -->
<!-- changed files:
- AGENTS.md
- Project.toml
- docs/plan.md
- src/stencil.jl
- test/oracle.jl
- test/test_stencil.jl
-->

<!-- session-handoff snapshot 2026-05-15T21:10:42Z -->
<!-- branch: main -->
<!-- changed files:
- .github/workflows/CI.yml
- AGENTS.md
- Project.toml
- docs/plan.md
- src/stencil.jl
- test/oracle.jl
- test/test_stencil.jl
-->

<!-- session-handoff snapshot 2026-05-15T21:11:57Z -->
<!-- branch: main -->
<!-- changed files:
- .github/workflows/CI.yml
- AGENTS.md
- Project.toml
- docs/plan.md
- src/stencil.jl
- test/oracle.jl
- test/test_stencil.jl
-->

<!-- session-handoff snapshot 2026-05-15T21:12:47Z -->
<!-- branch: main -->
<!-- changed files:
- .github/workflows/CI.yml
- AGENTS.md
- Project.toml
- docs/plan.md
- src/stencil.jl
- test/oracle.jl
- test/test_stencil.jl
-->

<!-- session-handoff snapshot 2026-05-15T21:12:50Z -->
<!-- branch: main -->
<!-- changed files:
- .github/workflows/CI.yml
- AGENTS.md
- Project.toml
- docs/plan.md
- src/stencil.jl
- test/oracle.jl
- test/test_stencil.jl
-->

<!-- session-handoff snapshot 2026-05-16T18:24:08Z -->
<!-- branch: main -->

<!-- session-handoff snapshot 2026-05-16T18:32:41Z -->
<!-- branch: main -->

<!-- session-handoff snapshot 2026-05-16T18:40:01Z -->
<!-- branch: main -->

<!-- session-handoff snapshot 2026-05-16T18:56:09Z -->
<!-- branch: main -->

<!-- session-handoff snapshot 2026-05-16T19:33:58Z -->
<!-- branch: main -->
<!-- changed files:
- AGENTS.md
- Project.toml
- docs/plan.md
- src/CartesianOperators.jl
- src/stencil.jl
- test/oracle.jl
- test/reference.jl
- test/runtests.jl
- test/test_stencil.jl
-->

<!-- session-handoff snapshot 2026-05-17T06:18:54Z -->
<!-- branch: main -->
<!-- changed files:
- AGENTS.md
- Project.toml
- docs/plan.md
- src/CartesianOperators.jl
- src/stencil.jl
- test/oracle.jl
- test/reference.jl
- test/runtests.jl
- test/test_stencil.jl
-->

<!-- session-handoff snapshot 2026-05-17T06:56:12Z -->
<!-- branch: main -->
<!-- changed files:
- AGENTS.md
- Project.toml
- README.md
- docs/plan.md
- src/CartesianOperators.jl
- src/stencil.jl
- test/oracle.jl
- test/reference.jl
- test/runtests.jl
- test/test_stencil.jl
-->
